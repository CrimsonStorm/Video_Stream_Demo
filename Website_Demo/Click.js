// Note that for a server this function will have a GET request to the DB in order to retreive the mp4 instead
// of the Video being hard coded in. It isn't that much different.

function showVideo() {
	
		document.getElementById("HUC").style.display = "flex";
}